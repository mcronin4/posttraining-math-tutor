[project]
name = "packages"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest-asyncio>=1.3.0",
    "tinker-cookbook>=0.1.0",
    "torch>=2.9.1",
    "transformers>=4.57.5",
]
