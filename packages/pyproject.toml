[project]
name = "packages"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "json-repair>=0.55.0",
    "pytest-asyncio>=1.3.0",
    "python-dotenv>=1.0.0",
    "tinker>=0.8.0",
    "tinker-cookbook @ git+https://github.com/ColinG03/tinker-cookbook.git@socratic-distillation",
    "torch>=2.9.1",
    "transformers>=4.45.0,<4.57.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["eval"]
